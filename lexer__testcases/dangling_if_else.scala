object Demo {
    def main(args: Array[String](2))={
    var i: Int = 0;
    var b: Int = 3;
    //var a: Array[Int](2) = {1,2};
    if (i<=3){
	b = b + i;
        //a[i]=a[i]+1;
    };
    if (i>=2){
	b = b + 2*i;
        //a[i]=a[i]-1;
    } else {
        b = 1;
    };
  };
};
